# Binaries and release outputs
/bin/
/dist/
/fyne-cross/

# Root-level built binaries (go build output)
/neofs-mount
/neofs-mount-tray
*.exe
*.AppImage
*.dmg
*.app

# Stray local builds (e.g. go build -o in repo root)
/neofs-mount
/neofs-mount-tray
/linuxdeploy*.AppImage

# IDEs and Tools
.idea/
.vscode/
*.swp
.DS_Store

# MacFUSE
.Spotlight-V100
.Trashes

# Fyne dependencies
Icon.png

# Test artifacts
/tmp/
*.test

# Windows PE resources: copied from win/pe-rsrc/ only for GOOS=windows builds (COFF .syso breaks Linux/cross links).
cmd/neofs-mount-tray/rsrc.syso
cmd/neofs-mount/rsrc.syso
